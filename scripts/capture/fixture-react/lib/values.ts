// the alias target ($game → lib): a path only the project's vite.config defines. The local plugin imports
// this, so a nonzero `alias` read back proves the host merged the project's resolve.alias.
export const aliasValue = 7;

