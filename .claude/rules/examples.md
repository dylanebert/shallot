---
paths:
    - "examples/**/*.ts"
    - "examples/**/*.scene"
    - "examples/AGENTS.md"
---

# Examples

Corpus/JSDoc drift/bloat: bugs. `examples/AGENTS.md`: problem/path/demo. Add/rename/delete entry + index together. Update in place, never -v2; delete unserved problems. Awkward recipes first indict API (`exports.md`).

## Recipes

One developer problem, not module. Minimal real defaults, no unrelated entities/scaffolding/float garbage; standard readability. Teach reusable API, comment only why. Demonstrate on open or world-space text hints interaction; no console-only/unhinted gestures.

Self-contained shallot.json/public/scenes/src plugins; no HTML/Vite, cross-recipe/repo-scripts imports (duplicate small shapes). Root tsc exercises API in manifest-declared plugin, not just untyped scenes. Dynamics need src/smoke.ts harness assertions + scripts/recipes.ts registration; compilation isn't behavior.

Physics recipes cite gym gold twins. Teach published substrate; beyond it, name twin/show supported expression. If none exists, explicitly use hatch + twin, never fake a lesser substitute.

## Other tiers

Gym: one project, query-selected subsystem atoms; correctness/benchmark/demo in one unbranched scene. Targeted device tier, not teaching. Fold labs case-by-case into owning params/modes, no mechanical moves. Real-input cases may own by-path Playwright drivers; params/assert stay environment-unaware.

Flows: verify-only ejected apps beyond unit reach. Showcase: rich self-contained capabilities, own/dogfood published-surface gates/driver, never repo scripts. Check tier before quality. Site builds published-package ejected consumers; command: root AGENTS.

Showcase perf: ProfilePlugin frame percentiles/GPU spans, real input for interaction costs. Gate named structural mechanisms/in-run ratios; absolute timing ungated. Attribute remote reports locally before gates; no local red: name dominant cost + ungated reading. RUM intake drives above/below 50ms, asserts duration-vital call AND intercepted wire; fulfill every matching request locally, never forward.

## Gym scenario contract

`examples/gym/src/gym.ts` owns params/build/optional assert/live. Params drive URL/CLI/UI rebuild/live knobs. Add file + barrel import; barrel: roster/coverage. Mirror readback/profiler timing, no CPU timer. Same orbit scene live/headless, deterministic start. Asserts own bounded condition advancement after profiler window (`testing.md`), never depend on its duration. Published harness ready/run yields Verdict ok/checks/metrics.

## Conventions

CLI runs manifests. Gym/visualization: HTML/Vite + bun dev; flows: HTML, verify-only. All ship public/icon.svg; HTML owners link it, CLI supplies manifest favicon. Native icon.png overrides. Dispose State on HMR/unmount. Package AGENTS UI rules apply; only ejected never-embedded gym/visualization may use fixed viewport. Complex UI: own-package svelte/@sveltejs/vite-plugin-svelte, svelte.config.js, mount/unmount.
