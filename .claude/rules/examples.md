---
paths:
    - "examples/**/*.ts"
    - "examples/**/*.scene"
    - "examples/AGENTS.md"
---

# Examples

Corpus/JSDoc drift/bloat: bugs. `examples/AGENTS.md`: problem/path/demo. Add/rename/delete entry + index together. Update in place, never -v2; delete unserved problems. Awkward recipes first indict API (`exports.md`).

## Recipes

One developer problem, not module. Minimal real defaults, no unrelated entities/scaffolding/float garbage; standard readability. Teach reusable API, comment only why. Demonstrate on open or world-space text hints interaction; no console-only/unhinted gestures.

Self-contained shallot.json/public/scenes/src plugins; no HTML/Vite, cross-recipe/repo-scripts imports (duplicate small shapes). Root tsc exercises API in manifest-declared plugin, not just untyped scenes.

## Other tiers

Flows: verify-only ejected apps beyond unit reach. Showcase: rich self-contained capabilities, own/dogfood published-surface gates/driver, never repo scripts. Check tier before quality.

Showcase perf: ProfilePlugin frame percentiles/GPU spans, real input for interaction costs. Gate named structural mechanisms/in-run ratios; absolute timing ungated. Attribute remote reports locally before gates; no local red: name dominant cost + ungated reading.

## Conventions

CLI runs manifests. Visualization: HTML/Vite + bun dev. All ship public/icon.svg; HTML owners link it, CLI supplies manifest favicon. Native icon.png overrides. Dispose State on HMR/unmount. Package AGENTS UI rules apply; only ejected never-embedded visualization may use fixed viewport. Complex UI: own-package svelte/@sveltejs/vite-plugin-svelte, svelte.config.js, mount/unmount.
