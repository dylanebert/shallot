# Archive

Retired units live in Git tags, not in the tree; check one out at its tag to read or rebuild it.

| Name | Tag | Path at tag | Retired because | Rebuild as |
|---|---|---|---|---|
| sandbox | `archive/sandbox-avbd` | `examples/showcase/sandbox` | It depended on the AVBD solver that left the core, and the showcase surface is being rebuilt with examples as the test surface. | The flagship showcase on built-in physics, composing recipes and running only gates a user project could run. |
| ocean | `archive/research-showcases` | `examples/showcase/ocean` | A research technique with its own oracles that reached repo-private helpers; not a user-shaped example. | A research repo of its own, or a showcase on public API only if a user problem demands it. |
| roads | `archive/research-showcases` | `examples/showcase/roads` | A research technique with its own oracles that reached repo-private helpers; not a user-shaped example. | A research repo of its own, or a showcase on public API only if a user problem demands it. |
| voxel | `archive/research-showcases` | `examples/showcase/voxel` | A research technique with its own oracles that reached repo-private helpers; not a user-shaped example. | A research repo of its own, or a showcase on public API only if a user problem demands it. |
| gym | `archive/test-surface-pre-slice` | `examples/gym` | Pre-slice test surface; replaced by declared tiers under the examples-as-the-test-surface philosophy. | Declared step/browser/built tiers; recipes regain checks through the reframe queue. |
| tests | `archive/test-surface-pre-slice` | `tests` | Pre-slice test surface; replaced by declared tiers under the examples-as-the-test-surface philosophy. | Declared step/browser/built tiers; recipes regain checks through the reframe queue. |
| recipe smoke plugins | `archive/test-surface-pre-slice` | `examples/recipes/*/src/smoke.ts` | Pre-slice test surface; replaced by declared tiers under the examples-as-the-test-surface philosophy. | Declared step/browser/built tiers; recipes regain checks through the reframe queue. |
| annotate-the-world | `archive/examples-pre-slice` | `examples/annotate-the-world` | Collapsed with billboards-and-sprites; world-space UI belongs in one demand recipe. | The frame area spec will rebuild the world-space UI claim as declared checks. |
| billboards-and-sprites | `archive/examples-pre-slice` | `examples/billboards-and-sprites` | Collapsed with annotate-the-world; world-space UI belongs in one demand recipe. | The frame area spec will rebuild the world-space UI claim as declared checks. |
| breakable-joints | `archive/examples-pre-slice` | `examples/breakable-joints` | Physics mechanism sample, not a demand recipe. | Physics closeout will rebuild the claim in checks beside `src/standard/physics`. |
| compute-and-readback | `archive/examples-pre-slice` | `examples/compute-and-readback` | Compute mechanism sample, not a demand recipe. | The frame area spec will rebuild the claim as declared checks beside the compute seam. |
| game-loop | `archive/examples-pre-slice` | `examples/game-loop` | Replaced by the smaller hello-world demand recipe. | The Engine core area spec will own the hello-world replacement and its declared checks. |
| joints | `archive/examples-pre-slice` | `examples/joints` | Physics mechanism sample, not a demand recipe. | Physics closeout will rebuild the claim in checks beside `src/standard/physics`. |
| moving-platform | `archive/examples-pre-slice` | `examples/moving-platform` | Physics mechanism sample, not a demand recipe. | Physics closeout will rebuild the claim in checks beside `src/standard/physics`. |
| physics-playground | `archive/examples-pre-slice` | `examples/physics-playground` | Physics playground sample, not a demand recipe. | Physics closeout will rebuild the claims in checks beside `src/standard/physics`. |
| ragdoll | `archive/examples-pre-slice` | `examples/ragdoll` | Physics mechanism sample, not a demand recipe. | Physics closeout will rebuild the claim in checks beside `src/standard/physics`. |
| surface-friction | `archive/examples-pre-slice` | `examples/surface-friction`, `examples/surface-friction/check.test.ts` | Physics mechanism sample; its two recipe check rows are retired with the recipe surface, and no row survives S1. | Physics closeout will rebuild the retired surface-friction claim in checks beside `src/standard/physics`. |
| roster machinery | `archive/test-surface-pre-slice` | `scripts/example-gates.ts, scripts/check-examples.ts, scripts/test-changed.ts, scripts/bench.ts` | Pre-slice test surface; replaced by declared tiers under the examples-as-the-test-surface philosophy. | Declared step/browser/built tiers; recipes regain checks through the reframe queue. |
| 0.9.5 compatibility corpus | `archive/test-surface-pre-slice` | `scripts/install-test/compat-0.9.5, scripts/install-test/compatibility.ts` | Pre-slice test surface; replaced by declared tiers under the examples-as-the-test-surface philosophy. | Declared step/browser/built tiers; recipes regain checks through the reframe queue. |
| verify | `archive/verify` | `bin/verify*, bin/{build,console-warning}.probes.ts, scripts/verify*, scripts/install-test/browser-server.ts` | Its frame-sampling and readiness heuristics (frame settles, center-vs-corner readiness, paint as identity) are what pillar 2 forbids. | `check`, reading the observation contract (pillar 3) with a browser driver in `src/harness`. |
| eval harness | `archive/verify` | `evals/harness` | A second private Playwright driver beside verify's. | Task gates declared in the browser tier and run by `check`. Tasks and setup now live in `dylanebert/shallot-bench`. |
| physics flat layout | `archive/physics-flat` | `src/standard/physics/engine` | The solver moved from 165 flat files into mechanism directories, and `api.ts` split into parts. | Nothing to rebuild: the tag is the pre-move reference for diff review. |
| tests-pre-slice | `archive/tests-pre-slice` | Inline tests under `src/` and `scripts/`, `src/testing/`, the ascii and visualization showcase Playwright suites, and six `scripts/check-*.ts` readers | Untiered tests and structural change detectors taxed every change without guarding the user surface. | Declared checks under the tier schema, re-admitted one claim at a time from this tag: physics gold replays first against the archived sample golds, then the six check invariants: boundary cone, subpath-only imports, dead exports, Node loadability, release version sites, script reachability. |
| physics sample golds | `archive/physics-samples` | `src/standard/physics/samples/` | The 40 minted trajectories had no live reader after the upstream sample corpus and gym were retired. | A recipe scenario port that replays the archived hashes, with the scenario's own declared check. |
| maintainer rules | `archive/rules-pre-0.10` | `.claude/rules` | Best practice belongs in the tool's shape: types, refusals, JSDoc, examples and checks, not prose an agent may or may not load. | One fresh `CONTRIBUTING.md`, with durable design rationale in a kex strategy note. |
| changelog and migration guide | `archive/changelog-pre-0.10` | `CHANGELOG.md, MIGRATION.md` | 0.10 is a clean point. | A fresh CHANGELOG whose first entry is 0.10 (major, recommended, pointing at this tag for older history) plus a new MIGRATION.md for 0.9→0.10, under agent tone review and human note review. |
| install test | `archive/install-test-pre-slice` | `scripts/install-test.ts, scripts/install-test.probes.ts, scripts/install-test/` | It exercised a projection-era package shape (`packages/` paths, the native Bun loader) through the archived `verify`, and nothing had run it since the collapse. | The built tier: pack, install into a scratch project, run that project's declared checks there. |
| ascii (pre-slice duplicate) | `archive/examples-pre-slice` | `examples/ascii` | Pre-slice duplicate of `render-to-a-terminal`; the scenes are byte-identical. | Never; the surviving `render-to-a-terminal` recipe is now the current `examples/ascii`. |
| build-a-scene | `archive/examples-pre-slice` | `examples/build-a-scene` | Create, add and set are the state mechanism; `src/engine` and `src/standard/loading` tests own it. | Never as a recipe; JSDoc on `State.create` and `warm`. |
| fog-and-light-shafts | `archive/examples-pre-slice` | `examples/fog-and-light-shafts` | Module tour: two knobs on load, a scene naming fields. | Rendered check beside `src/standard/fog`; a recipe only if a stall earns it (pillar 7). |
| measure-performance | `archive/examples-pre-slice` | `examples/measure-performance` | Timings populating is `src/extras/profile`'s own claim; the guard is JSDoc. | Never as a recipe; a unit test beside `src/extras/profile`. |
| orbit-camera | `archive/examples-pre-slice` | `examples/orbit-camera` | Module tour: two feel knobs and a caption. | Never as a recipe; a drag-to-yaw unit test beside `src/extras/orbit`. |
| stylize-the-look | `archive/examples-pre-slice` | `examples/stylize-the-look` | Module tour of Outline; the band is `src/extras/outline`'s rendered claim. | Rendered check beside `src/extras/outline`; a recipe only if a stall earns it (pillar 7). |
| visualization | `archive/examples-pre-slice` | `examples/visualization` | Primitive gallery with its own vite, nothing a person plays; its historical companion `annotate-the-world` is archived above. | Never; a debug-overlay recipe only if a stall earns it. |
