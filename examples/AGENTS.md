# Examples

Generated by `bun run examples:index` from each `examples/<name>/shallot.json`; edit the manifest, not this file. Run one with `bunx shallot dev examples/<name>`.

## Recipes

| name | description | add |
| --- | --- | --- |
| `animate-with-clips/` | Play animation clips with Animator playables. | `bunx shallot add animate-with-clips` |
| `annotate-the-world/` | Annotate the world with text, lines and arrows. | `bunx shallot add annotate-the-world` |
| `billboards-and-sprites/` | Draw billboards and sprites, with their modes and a meter. | `bunx shallot add billboards-and-sprites` |
| `breakable-joints/` | Break joints under load with escape-hatch events. | `bunx shallot add breakable-joints` |
| `compute-and-readback/` | Run a compute pass and read the result back with slab and Mirror. | `bunx shallot add compute-and-readback` |
| `custom-material/` | Write a custom WGSL surface or backdrop material. | `bunx shallot add custom-material` |
| `day-night-sky/` | Run a procedural sky whose sun follows the time of day. | `bunx shallot add day-night-sky` |
| `drive-a-vehicle/` | Drive a vehicle with W/S/A/D through the Physics escape hatch. | `bunx shallot add drive-a-vehicle` |
| `first-person/` | Walk a first-person character controller across platforms. | `bunx shallot add first-person` |
| `game-loop/` | Run game logic every frame as an ECS system inside a plugin. | `bunx shallot add game-loop` |
| `gpu-particles/` | Simulate particles in compute and draw them from the vertex buffer. | `bunx shallot add gpu-particles` |
| `import-a-model/` | Import a glTF model: mesh refs, loading and placement. | `bunx shallot add import-a-model` |
| `joints/` | Connect bodies with Spring and Joint. | `bunx shallot add joints` |
| `moving-platform/` | Carry bodies on a kinematic moving platform. | `bunx shallot add moving-platform` |
| `overlay-ui/` | Overlay or embed UI on the canvas with mountOverlay or run. | `bunx shallot add overlay-ui` |
| `physics-playground/` | Drop physics bodies and joints into a scene. | `bunx shallot add physics-playground` |
| `play-sound/` | Play spatial sound from sources to a listener. | `bunx shallot add play-sound` |
| `ragdoll/` | Turn a skinned model into a ragdoll with LiveSkin and the Physics escape hatch. | `bunx shallot add ragdoll` |
| `render-to-a-terminal/` | Render the scene to a character-cell grid with the Cells plugin. | `bunx shallot add render-to-a-terminal` |
| `respond-to-input/` | Respond to held keys, key presses and the mouse. | `bunx shallot add respond-to-input` |
| `save-and-restore/` | Save and restore a scene as XML in storage. | `bunx shallot add save-and-restore` |
| `surface-friction/` | Control how bodies slide with Body.friction. | `bunx shallot add surface-friction` |
