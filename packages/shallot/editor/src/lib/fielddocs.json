{
  "ambient-light": {
    "summary": "ambient light component. sear's `lit` / `lightFactor` helpers read it via the kitchen Lighting uniform. `color` is hex sRGB (e.g. `0xd0dcec`); `intensity` is a linear multiplier",
    "fields": {
      "color": {
        "description": null,
        "type": "color",
        "default": "0xffffff"
      },
      "intensity": {
        "description": null,
        "type": "float",
        "default": "0.5"
      }
    }
  },
  "arrow": {
    "summary": "an arrowhead on a {@link Line}: four world-space fins (Bevy's fletched shape) at the line's endpoints. Requires a {@link Line} on the same entity",
    "fields": {
      "start": {
        "description": "a head at the start endpoint when nonzero",
        "type": "float",
        "default": "0"
      },
      "end": {
        "description": "a head at the end endpoint when nonzero",
        "type": "float",
        "default": "1"
      },
      "size": {
        "description": "head size relative to the shaft length",
        "type": "float",
        "default": "1"
      }
    }
  },
  "backdrop": {
    "summary": "select a Sear camera's backdrop: the {@link Backgrounds} recipe drawn behind the scene as a fullscreen view-ray → color fill on the un-rendered pixels. Without it the camera shows the flat `Camera.clearColor` (the opt-in fallback). The recipe is registered in code (`Backgrounds.register`); this picks one per camera by name.",
    "fields": {
      "name": {
        "description": "the registered background drawn behind the scene (selected by name)",
        "type": "float",
        "default": ""
      }
    }
  },
  "body": {
    "summary": "a rigid body simulated by the solver: falls under gravity and collides with other bodies (`mass: 0` = static).",
    "fields": {
      "shape": {
        "description": "the collider, a `ShapeKind`: `Box` (an OBB of `halfExtents`), `Sphere`, `Capsule` (a segment along local Y inflated by the radius), or `Hull` (a convex polytope registered in `Hulls`).",
        "type": "float",
        "default": "0"
      },
      "pos": {
        "description": "spawn position; the solver owns it after spawn.",
        "type": "vec3",
        "default": "0 0 0"
      },
      "quat": {
        "description": "spawn orientation, authored as euler degrees like `Transform.rot`; physics-owned after spawn.",
        "type": "vec3",
        "default": "0 0 0"
      },
      "half-extents": {
        "description": "box/AABB half-extents in `xyz`; `w` doubles as the rounding radius (sphere/capsule) or the `Hull` id (a hull has radius 0, so the lane is free).",
        "type": "vec3",
        "default": "0.5 0.5 0.5"
      },
      "mass": {
        "description": "mass in kg; `0` or less marks a static body that never moves.",
        "type": "float",
        "default": "1"
      },
      "friction": {
        "description": "coulomb friction coefficient: `0` slides freely, higher grips.",
        "type": "float",
        "default": "0.5"
      }
    }
  },
  "camera": {
    "summary": "camera component. Pose comes from {@link Transform} (looks down its local -Z). A lone camera auto-binds to the first `<canvas>` in the document, so the single-view case needs no wiring; multi-view (or a dynamically-created canvas) binds each camera explicitly via `attachCanvas` from `render/core`. `clearColor` is hex sRGB-encoded (e.g. `0x5cbfbf`); sear unpacks to linear when recording the camera's render pass",
    "fields": {
      "mode": {
        "description": null,
        "type": "perspective | orthographic",
        "default": "perspective"
      },
      "fov": {
        "description": null,
        "type": "float",
        "default": "60"
      },
      "near": {
        "description": null,
        "type": "float",
        "default": "0.1"
      },
      "far": {
        "description": null,
        "type": "float",
        "default": "1000"
      },
      "size": {
        "description": null,
        "type": "float",
        "default": "5"
      },
      "clear-color": {
        "description": null,
        "type": "color",
        "default": "0x2e2b28"
      },
      "antialias": {
        "description": "antialiasing: 1 = 4× MSAA (default), 0 = off (single-sample, crisp, for a pixel-art look)",
        "type": "float",
        "default": "1"
      }
    }
  },
  "character": {
    "summary": "a kinematic character: a capsule {@link Body} (`mass <= 0`) swept against the scene's bodies each fixed step (collide-and-slide on the CPU). Authors the walkable slope, the jump launch speed, and a per-character gravity; the controller sweeps every `[Character, Body]`, drives it via {@link move} / {@link jump}, and reads {@link grounded} / {@link pose} back. The first-person {@link Player} composes this.",
    "fields": {
      "max-slope": {
        "description": "steepest walkable slope in degrees; a contact flatter than this grounds the character, steeper it slides",
        "type": "float",
        "default": "45"
      },
      "jump-speed": {
        "description": "the launch velocity a buffered + grounded {@link jump} sets. 0 disables jumping",
        "type": "float",
        "default": "0"
      },
      "gravity": {
        "description": "per-character gravity (negative, snappier than the world for a player). 0 = the configured world gravity",
        "type": "float",
        "default": "0"
      }
    }
  },
  "color": {
    "summary": "per-entity base color. Authored CPU-side as a linear {@link Quad}, but mirrored to the GPU as one sRGB-packed u32 ({@link srgb8x4}, 16 B → 4 B), published to `Compute.buffers` under the name `\"color\"`; a surface reads it as `unpackLdrColor(color[eid])`. Alpha is reserved for transparency",
    "fields": {
      "rgba": {
        "description": null,
        "type": "vec3",
        "default": "1 1 1"
      }
    }
  },
  "depth": {
    "summary": "opt a Sear camera into the **depth lane**: the prepass *stores* its single-sample depth and publishes it as `view.depth` (without this marker the prepass depth is discarded, never reaching main memory). Requestable on its own (a depth-only consumer needs no id) or alongside {@link Tag} (one prepass writes both). Bevy's `DepthPrepass`. A screen-space consumer (AO, fog, volumetrics) adds it to read `view.depth`.",
    "fields": {}
  },
  "directional-light": {
    "summary": "directional light component. sear's `lit` / `lightFactor` helpers read it via the kitchen Lighting uniform. `direction` is the light's travel direction (down-pointing for a sun overhead); auto-normalized when packed",
    "fields": {
      "color": {
        "description": null,
        "type": "color",
        "default": "0xffffff"
      },
      "intensity": {
        "description": null,
        "type": "float",
        "default": "1.5"
      },
      "direction": {
        "description": null,
        "type": "vec3",
        "default": "-0.6 -1 -0.8"
      }
    }
  },
  "fog": {
    "summary": "the scene's volumetric atmosphere: one per scene (a singleton). The fog pass marches each pixel from the camera to the scene depth, accumulating extinction and fading the scene toward `color`. `density` is the base haze thickness; `heightFalloff` makes it an exponential height fog (denser low, thinning with altitude above `heightBase`); `steps` / `jitter` trade march cost for banding. The scattering knobs (`absorption` / `scattering` / `anisotropy` / `scatterIntensity`) shape volumetric light shafts.",
    "fields": {
      "density": {
        "description": "base extinction coefficient: how fast the scene fades into haze with distance (0 = clear)",
        "type": "float",
        "default": "0.02"
      },
      "color": {
        "description": "hex sRGB haze color the scene fades toward (e.g. 0xb5c4d8)",
        "type": "color",
        "default": "0xb5c4d8"
      },
      "absorption": {
        "description": "absorbed fraction of extinction [0,1]; the rest scatters (the scattering albedo for light shafts)",
        "type": "float",
        "default": "0"
      },
      "scattering": {
        "description": "in-scatter strength: how brightly light shafts glow in the haze",
        "type": "float",
        "default": "1"
      },
      "anisotropy": {
        "description": "Henyey-Greenstein anisotropy [-1,1]: 0 even glow, →1 forward (bright halo toward a light)",
        "type": "float",
        "default": "0"
      },
      "height-base": {
        "description": "world height where density equals `density`: the base of the height falloff",
        "type": "float",
        "default": "0"
      },
      "height-falloff": {
        "description": "exponential density falloff per world unit above `heightBase` (0 = uniform haze, no height fog)",
        "type": "float",
        "default": "0"
      },
      "steps": {
        "description": "raymarch step count along each pixel's ray (clamped to 256); more = smoother, costlier",
        "type": "float",
        "default": "32"
      },
      "jitter": {
        "description": "per-pixel step jitter [0,1] that breaks march banding into noise (0 = fixed midpoint sampling)",
        "type": "float",
        "default": "1"
      },
      "scatter-intensity": {
        "description": "overall multiplier on in-scatter brightness",
        "type": "float",
        "default": "1"
      }
    }
  },
  "glaze": {
    "summary": "per-camera postfx tuning. A camera tonemaps Neutral by default (no `Glaze` needed); add `Glaze` to pick a different {@link Tonemap} operator, dial a color grade, or enable vignette / posterize / dither. `tonemap` is a {@link Tonemap} index (0 = Neutral default, 1 = None); `exposure` scales the scene pre-tonemap; the grade is ASC CDL `slope`/`offset`/`power` (per-channel rgb, scene-referred, pre-tonemap) plus a post-tonemap `saturation`; `vignette` is corner darkness in [0,1] between `vignetteInner` and `vignetteOuter` screen radii; `posterize` is the band count (0 = off) and `dither` the OkLab-L dither amplitude that breaks bands. The grade defaults to a no-op (slope/power 1, offset 0, saturation 1).",
    "fields": {
      "exposure": {
        "description": null,
        "type": "float",
        "default": "1"
      },
      "tonemap": {
        "description": null,
        "type": "float",
        "default": "0"
      },
      "slope": {
        "description": null,
        "type": "vec3",
        "default": "1 1 1"
      },
      "offset": {
        "description": null,
        "type": "vec3",
        "default": "0 0 0"
      },
      "power": {
        "description": null,
        "type": "vec3",
        "default": "1 1 1"
      },
      "saturation": {
        "description": null,
        "type": "float",
        "default": "1"
      },
      "vignette": {
        "description": null,
        "type": "float",
        "default": "0"
      },
      "vignette-inner": {
        "description": null,
        "type": "float",
        "default": "0"
      },
      "vignette-outer": {
        "description": null,
        "type": "float",
        "default": "1"
      },
      "posterize": {
        "description": null,
        "type": "float",
        "default": "0"
      },
      "dither": {
        "description": null,
        "type": "float",
        "default": "0"
      }
    }
  },
  "joint": {
    "summary": "a hard joint pinning two bodies together: a rigid linear pin plus an optional angular lock, referencing both by `@name`.",
    "fields": {
      "a": {
        "description": "the first body (a `@name` reference).",
        "type": "entity",
        "default": "0"
      },
      "b": {
        "description": "the second body.",
        "type": "entity",
        "default": "0"
      },
      "r-a": {
        "description": "the pin's anchor on body `a`, in its local frame.",
        "type": "vec3",
        "default": "0 0 0"
      },
      "r-b": {
        "description": "the pin's anchor on body `b`, in its local frame.",
        "type": "vec3",
        "default": "0 0 0"
      },
      "stiffness-ang": {
        "description": "angular lock: `0` (default) leaves rotation free (spherical); `∞` locks orientation (author `stiffness-ang: fixed`).",
        "type": "float",
        "default": "0"
      }
    }
  },
  "line": {
    "summary": "a debug line anchored to an entity, drawn from its {@link Transform} position along a world-rotated offset. A retained scene annotation, expanded into one screen-space segment each frame",
    "fields": {
      "offset": {
        "description": "line vector from the entity in its local frame, rotated by the transform (`0 1 0` = one unit up)",
        "type": "vec3",
        "default": "1 0 0"
      },
      "thickness": {
        "description": "constant screen width in pixels",
        "type": "float",
        "default": "2"
      },
      "color": {
        "description": "hex sRGB color",
        "type": "color",
        "default": "0xffffff"
      },
      "opacity": {
        "description": "0..1 opacity multiplier",
        "type": "float",
        "default": "1"
      },
      "visible": {
        "description": "drawn when nonzero; set to 0 to hide without removing (edit-mode safe)",
        "type": "float",
        "default": "1"
      }
    }
  },
  "listener": {
    "summary": "marks the spatial listener entity. its `Transform` orients the FOA + HRTF render",
    "fields": {}
  },
  "material": {
    "summary": "per-entity PBR material the `default` / `vertex` surfaces read (alongside `Color`, the base albedo). One slab `Quad` published as `\"material\"`, lanes `(metallic, roughness, emissive, occlusion)`: `metallic` and `roughness` are the metallic-roughness knobs ([0,1]); `emissive` is a glow **strength** tinting the base color (`Color.rgb * emissive`); `occlusion` dims ambient ([0,1]). Defaults are flat (metallic 0, roughness 1, emissive 0, occlusion 1), so a Part without it shades exactly like the pre-PBR diffuse `lit`. Independent (non-tinted) emissive + texture-driven maps are the glTF importer's job (it drives sear's `litPbr` from its own per-material palette).",
    "fields": {
      "params": {
        "description": "the four PBR lanes `(metallic, roughness, emissive, occlusion)`, authored named via the `material` attribute (`material=\"metallic: 1; roughness: 0.2\"`).",
        "type": "vec4",
        "default": "0 1 0 1"
      }
    }
  },
  "orbit": {
    "summary": "orbit camera controls: drag to rotate around a target, scroll to zoom",
    "fields": {
      "yaw": {
        "description": "horizontal orbit angle around the target, radians",
        "type": "deg | rad",
        "default": "30 deg"
      },
      "pitch": {
        "description": "vertical orbit angle, radians; clamped to min/maxPitch",
        "type": "deg | rad",
        "default": "20 deg"
      },
      "distance": {
        "description": "camera distance from the target, world units (perspective zoom)",
        "type": "float",
        "default": "10"
      },
      "size": {
        "description": "orthographic half-height, world units (ortho zoom)",
        "type": "float",
        "default": "5"
      },
      "min-pitch": {
        "description": "lower pitch clamp, radians",
        "type": "deg | rad",
        "default": "-89 deg"
      },
      "max-pitch": {
        "description": "upper pitch clamp, radians",
        "type": "deg | rad",
        "default": "89 deg"
      },
      "min-distance": {
        "description": "closest perspective distance",
        "type": "float",
        "default": "0.1"
      },
      "max-distance": {
        "description": "farthest perspective distance",
        "type": "float",
        "default": "900"
      },
      "min-size": {
        "description": "smallest orthographic size",
        "type": "float",
        "default": "0.05"
      },
      "max-size": {
        "description": "largest orthographic size",
        "type": "float",
        "default": "900"
      },
      "smoothness": {
        "description": "follow damping, 0–1; higher snaps to the target pose faster",
        "type": "float",
        "default": "0.3"
      },
      "fly-smoothness": {
        "description": "fly look damping, 0–1; higher is snappier; default tighter than orbit so first-person look tracks closely",
        "type": "float",
        "default": "0.6"
      },
      "sensitivity": {
        "description": "orbit look speed (yaw/pitch), radians per pixel of mouse drag",
        "type": "float",
        "default": "0.005"
      },
      "fly-sensitivity": {
        "description": "fly look speed (yaw/pitch), radians per pixel; separate so fly look reads calmer than orbit",
        "type": "float",
        "default": "0.003"
      },
      "zoom-speed": {
        "description": "zoom factor applied per scroll-wheel notch",
        "type": "float",
        "default": "0.025"
      },
      "orbit-button": {
        "description": "mouse button that orbits: 0 left, 1 middle, 2 right",
        "type": "float",
        "default": "0"
      },
      "pan-button": {
        "description": "mouse button that pans: 0 left, 1 middle, 2 right",
        "type": "float",
        "default": "1"
      },
      "fly-button": {
        "description": "mouse button that flies (hold to look around, WASD/QE to move): 0 left, 1 middle, 2 right",
        "type": "float",
        "default": "2"
      },
      "pan": {
        "description": "pan offset from the orbit target, world units",
        "type": "vec3",
        "default": "0 0 0"
      },
      "fly-speed": {
        "description": "WASD/QE fly speed, world units per second; scroll while flying adjusts it (clamped to flyMin/flyMax)",
        "type": "float",
        "default": "5"
      },
      "fly-boost": {
        "description": "shift-held fly boost multiplier, transient; scales flySpeed while shift is down, never stored",
        "type": "float",
        "default": "3"
      },
      "fly-min": {
        "description": "lower clamp for scroll-adjusted flySpeed, world units per second",
        "type": "float",
        "default": "0.5"
      },
      "fly-max": {
        "description": "upper clamp for scroll-adjusted flySpeed, world units per second",
        "type": "float",
        "default": "100"
      },
      "mode": {
        "description": "Free orbits, pans, and zooms; Locked disables orbit rotation, leaving pan and zoom",
        "type": "free | locked",
        "default": "free"
      },
      "target": {
        "description": "entity to orbit; pan is relative to its position (0 = world origin)",
        "type": "entity",
        "default": "0"
      }
    }
  },
  "outline": {
    "summary": "outline highlight: a colored band hugs the object's silhouette for hover, selection, or grab feedback.",
    "fields": {
      "color": {
        "description": "band color, linear rgb (alpha unused in v1)",
        "type": "vec3",
        "default": "1 0.85 0.2"
      },
      "width": {
        "description": "band thickness in pixels, clamped to 64",
        "type": "float",
        "default": "4"
      },
      "occlude": {
        "description": "0 = always-on-top (default); 1 = occlusion-aware, hidden where the object is behind other geometry (needs sear's `Depth` on the camera)",
        "type": "float",
        "default": "0"
      }
    }
  },
  "part": {
    "summary": "ECS-shaped opt-in for Part rendering. `surface` holds the {@link Surfaces} ID for the entity's shading; `mesh` holds the {@link Meshes} ID for its geometry: both `slab(u32)` the pack reads on GPU. The pack groups Parts by `(surface, mesh)` and emits one indirect draw per used pair, so a surface is shading only and renders any mesh. `surface` defaults to `\"default\"`, `mesh` to `\"cube\"`; scenes pick others via `<a part=\"surface: checker; mesh: wall\" />`",
    "fields": {
      "surface": {
        "description": null,
        "type": "float",
        "default": "0"
      },
      "mesh": {
        "description": null,
        "type": "float",
        "default": "0"
      }
    }
  },
  "player": {
    "summary": "first-person player: the look + camera layer over a kinematic {@link Character}. Lives on the same capsule {@link Body} as a {@link Character} (`mass <= 0`); the character module registers + drives it, this adds the mouse look + a follow camera. `camera` is the eid of a separate camera entity (Camera + Transform + a renderer marker) the controller poses each frame. `distance` is the camera's pull-back from the eye. 0 is first-person (the default), `> 0` is the third-person scaffolding. Walk/jump/slope tuning lives on `Character`.",
    "fields": {
      "yaw": {
        "description": "look yaw in radians (turn around world Y); set it to face a direction at spawn",
        "type": "float",
        "default": "0"
      },
      "pitch": {
        "description": "look pitch in radians (clamped to ±90°); set it to tilt the view at spawn",
        "type": "float",
        "default": "0"
      },
      "speed": {
        "description": "walk speed (m/s) the move input is scaled to",
        "type": "float",
        "default": "6"
      },
      "sprint": {
        "description": "sprint multiplier applied while Shift is held",
        "type": "float",
        "default": "1.8"
      },
      "sensitivity": {
        "description": "mouse-look radians per pixel of pointer-lock movement, at a fixed 1080px reference height (the look speed is resolution-independent, so the same mouse motion turns the same angle at any canvas size)",
        "type": "float",
        "default": "1.5"
      },
      "eye-height": {
        "description": "camera height above the capsule centre (the eye offset)",
        "type": "float",
        "default": "0.7"
      },
      "distance": {
        "description": "camera pull-back from the eye: 0 = first-person, > 0 = third-person (scaffolding)",
        "type": "float",
        "default": "0"
      },
      "camera": {
        "description": "the linked camera entity (a Camera + Transform); set this or the camera never moves",
        "type": "entity",
        "default": "0"
      }
    }
  },
  "point-light": {
    "summary": "point light component. Position comes from the entity's `Transform`; sear's `lit` / `lightFactor` helpers accumulate the fragment's cluster's point lights: inverse-square falloff windowed smoothly to exactly zero at `range`. `color` is hex sRGB; `intensity` is a linear multiplier. Slab fields: the light-cull compute passes read them straight off the GPU (no CPU light list)",
    "fields": {
      "color": {
        "description": "the light's hex sRGB color (e.g. 0xffd9a0)",
        "type": "color",
        "default": "0xffffff"
      },
      "intensity": {
        "description": "linear brightness multiplier",
        "type": "float",
        "default": "1"
      },
      "range": {
        "description": "the distance (metres) the falloff smoothly reaches zero at: the cull cutoff",
        "type": "float",
        "default": "10"
      },
      "radius": {
        "description": "the physical source radius (metres): a soft sphere, not a point. Larger softens the near-field bulb and widens the specular highlight; 0.01 reproduces the old bare-filament hotspot",
        "type": "float",
        "default": "0.1"
      }
    }
  },
  "resolution": {
    "summary": "render this camera at a fixed low resolution and scale it up to fill the canvas, crisp not blurred. Without it the view renders at the canvas backing size (the global pixelRatio policy). `0` on an axis derives it from the other to keep the canvas aspect, so `height: 360` alone renders 360 lines tall and as wide as the canvas shape needs; set both for an exact (possibly aspect-distorting) target. Per camera, so each canvas in a multi-view scene pins its own. Pairs with {@link Camera} `antialias` off.",
    "fields": {
      "width": {
        "description": "render width in pixels; 0 = derive from height to keep the canvas aspect",
        "type": "float",
        "default": "0"
      },
      "height": {
        "description": "render height in pixels; 0 = derive from width to keep the canvas aspect",
        "type": "float",
        "default": "0"
      }
    }
  },
  "sear": {
    "summary": "marker selecting Sear as the active renderer on a Camera entity. A camera carrying it renders through sear's color pass, plus the opt-in prepass lanes its {@link Tag} / {@link Depth} markers request. Lives in the renderer impl with the systems that query it; the thin `sear` barrel re-exports it to the game author, `sear/core` re-exports the systems to an extender.",
    "fields": {}
  },
  "sequence": {
    "summary": "a timeline that plays {@link Tween}s placed on it with `at`: a shared playhead many tweens read",
    "fields": {
      "state": {
        "description": "play control; a fresh sequence defaults to playing",
        "type": "idle | playing | complete",
        "default": "playing"
      },
      "elapsed": {
        "description": "playhead position, seconds; the tweens on it read their slice as `elapsed - at`",
        "type": "float",
        "default": "0"
      },
      "duration": {
        "description": "timeline period, seconds; grows to span its tweens' ends",
        "type": "float",
        "default": "0"
      },
      "loop": {
        "description": "wrap the whole timeline back to the start on completion",
        "type": "float",
        "default": "0"
      }
    }
  },
  "shadow": {
    "summary": "shadow tuning, on a light entity: presence is the switch (like {@link Tag} on a camera), the fields are the tuning. On the {@link DirectionalLight} it casts the sun's cascaded shadow map; on a {@link PointLight} the light's six cube faces render into the shared depth atlas (`distance` doesn't apply there: coverage is the light's `range`, and the tile size is importance-sized from the {@link PointShadows} atlas budget). `distance` is the directional sun's **max shadow distance**: the camera's view range is split into cascades out to it ({@link SunShadows}); raise it to shadow farther, lower it for finer near texels. `normalBias` is the primary acne fix: the receiver is shifted along its surface normal by `normalBias` shadow-map texels (in world size) before the depth compare, so grazing faces (where acne is worst) get more offset (raise it if acne shows, lower it if shadows detach from contact edges). `depthBias` is a small residual depth bias toward the light the normal offset can't cover (flat faces dead-on to the light).",
    "fields": {
      "distance": {
        "description": "the sun's max shadow distance: the camera view range is split into cascades out to it; raise to shadow farther, lower for finer near texels. Ignored on a point light (coverage is its `range`).",
        "type": "float",
        "default": "50"
      },
      "depth-bias": {
        "description": "a small residual depth bias toward the light, covering flat faces dead-on to it the normal offset can't.",
        "type": "float",
        "default": "0.001"
      },
      "normal-bias": {
        "description": "the primary acne fix: shifts the receiver along its surface normal by this many shadow texels before the depth compare. Raise if acne shows, lower if shadows detach at contact edges.",
        "type": "float",
        "default": "1.8"
      }
    }
  },
  "skin": {
    "summary": "a skinned glTF instance's animation state. One `slab(vec4)` published as `\"skin\"`: lane x is the play time in seconds (advanced by {@link SkinSystem}), y the material palette index in the shared union palette (asset base + local id, the `mid` the shade path reads, folded in to keep the surface at the 10-storage ceiling), z a per-instance phase offset (crowd variety; 0 for a single import), w the clip duration {@link SkinSystem} loops the play time on. The duration is per-instance, so N skinned meshes with different clip lengths coexist in one scene. The importer adds it to each skinned instance.",
    "fields": {
      "anim": {
        "description": null,
        "type": "vec3",
        "default": "0 0 0"
      }
    }
  },
  "sky": {
    "summary": "the scene's procedural sky, one per scene (a singleton). A camera shows it by selecting the registered `sky` backdrop (`backdrop=\"name: sky\"`). The look is a layered recipe: an elevation gradient from `horizon` up to `zenith`, a sun glow + disk (positioned by the scene's directional light, tinted `sun-color`), FBM `cloud`s, hash-grid `star`s, and a `haze` band fading the horizon. The sun's direction follows the directional light; this component sets only its appearance.",
    "fields": {
      "zenith": {
        "description": "hex sRGB color overhead, at the zenith (e.g. 0x89b6e9)",
        "type": "color",
        "default": "0x89b6e9"
      },
      "horizon": {
        "description": "hex sRGB color at the horizon, blended up toward `zenith`",
        "type": "color",
        "default": "0xc4cdda"
      },
      "band": {
        "description": "bright band strength right at the horizon line [0,1] (0 = none)",
        "type": "float",
        "default": "0"
      },
      "sun-color": {
        "description": "hex sRGB tint of the sun glow + disk (the sun's *position* follows the directional light)",
        "type": "color",
        "default": "0xffffff"
      },
      "sun-size": {
        "description": "sun disk size [0,1]: larger paints a bigger disk",
        "type": "float",
        "default": "0.7"
      },
      "sun-glow": {
        "description": "sun glow strength around the disk [0,1] (0 = no glow)",
        "type": "float",
        "default": "0.5"
      },
      "cloud-color": {
        "description": "hex sRGB cloud color",
        "type": "color",
        "default": "0xffffff"
      },
      "cloud-coverage": {
        "description": "cloud coverage [0,1]: how much of the sky the clouds fill (0 = clear)",
        "type": "float",
        "default": "0.5"
      },
      "cloud-density": {
        "description": "cloud opacity / thickness [0,1]",
        "type": "float",
        "default": "0.7"
      },
      "cloud-height": {
        "description": "cloud layer height: scales the projected cloud size (larger = higher, smaller clouds)",
        "type": "float",
        "default": "4"
      },
      "star-intensity": {
        "description": "star brightness [0,1] (0 = no stars)",
        "type": "float",
        "default": "0"
      },
      "star-amount": {
        "description": "star density [0,1]: more stars in the grid",
        "type": "float",
        "default": "0.5"
      },
      "haze-color": {
        "description": "hex sRGB haze color the horizon fades toward",
        "type": "color",
        "default": "0xbcc5d4"
      },
      "haze-density": {
        "description": "horizon haze strength [0,1] (0 = none)",
        "type": "float",
        "default": "0.005"
      }
    }
  },
  "sound": {
    "summary": "a playing sound, mirroring Bevy's `AudioPlayer` + `PlaybackSettings`: add it to start, remove it to stop a loop. `instrument` is a registered instrument id; `loop` 0 = one-shot (frees itself when the envelope idles), 1 = loop; `volume` (0–1, quadratic) and `pitch` (semitones, oscillator instruments only) firehose live to the voice. `voice` is the allocated voice handle, managed by {@link SoundSystem}; don't write it",
    "fields": {
      "instrument": {
        "description": "registered instrument or sample name, resolved to an id (a bare sample auto-wraps a sampler)",
        "type": "float",
        "default": "-1"
      },
      "loop": {
        "description": "0 = one-shot (frees itself when the envelope idles), 1 = loop until the `Sound` is removed",
        "type": "float",
        "default": "0"
      },
      "volume": {
        "description": "playback level 0–1, quadratic; firehoses live to the voice",
        "type": "float",
        "default": "1"
      },
      "pitch": {
        "description": "pitch offset in semitones (oscillator instruments only); firehoses live to the voice",
        "type": "float",
        "default": "0"
      },
      "voice": {
        "description": "allocated voice handle, managed by the audio system. read-only, don't author",
        "type": "float",
        "default": "-1"
      }
    }
  },
  "spot": {
    "summary": "spot add-on for a {@link PointLight}: presence narrows the light into a cone (like {@link Shadow}, presence is the switch). The cone points along the entity's forward axis (its `Transform` rotation), so aim it by rotating the entity. `inner` / `outer` are half-angles in degrees (axis to edge): full brightness inside `inner`, smoothly to dark at `outer`. The light still falls off + culls by the PointLight's `range`.",
    "fields": {
      "inner": {
        "description": "the cone's inner half-angle (degrees, axis→edge): full brightness inside it",
        "type": "float",
        "default": "20"
      },
      "outer": {
        "description": "the cone's outer half-angle (degrees, axis→edge): dark past it, smooth between inner and outer",
        "type": "float",
        "default": "30"
      }
    }
  },
  "spring": {
    "summary": "a soft distance spring linking two bodies, pulling them toward a rest length; its own entity, referencing the bodies by `@name`.",
    "fields": {
      "a": {
        "description": "the first body (a `@name` reference).",
        "type": "entity",
        "default": "0"
      },
      "b": {
        "description": "the second body.",
        "type": "entity",
        "default": "0"
      },
      "r-a": {
        "description": "anchor point on body `a`, in its local frame.",
        "type": "vec3",
        "default": "0 0 0"
      },
      "r-b": {
        "description": "anchor point on body `b`, in its local frame.",
        "type": "vec3",
        "default": "0 0 0"
      },
      "stiffness": {
        "description": "pull strength; higher is stiffer.",
        "type": "float",
        "default": "100"
      },
      "rest": {
        "description": "the target distance the spring pulls the anchors toward.",
        "type": "float",
        "default": "1"
      }
    }
  },
  "sprite": {
    "summary": "a textured world-space quad (icon, marker) anchored to an entity's {@link Transform}. `image` is a registered image id ({@link image}), `size` the world-space quad size, `anchor` the 0..1 pivot (0.5 0.5 = centered), `color` a hex sRGB tint, `billboard` a {@link SpriteBillboard} mode, `blend` a {@link SpriteBlend} mode. `opacity` multiplies the texture alpha; under the default `clip` blend that shrinks the cutout (the sprite vanishes below 0.5, the gltf-clip convention); a smooth fade needs `blend: alpha`. The quad scales by the transform's scale on top of `size`. `fill` shows only the leading 0..1 fraction of the image along a {@link SpriteFill} `fillMode`: a radial fill over a ring icon is a progress ring, a vertical fill over a bar icon a gauge",
    "fields": {
      "image": {
        "description": "registered image id (see {@link image}); a scene's `image:` resolves the registered name",
        "type": "float",
        "default": "0"
      },
      "size": {
        "description": "quad size in world units, before the transform's scale",
        "type": "vec2",
        "default": "1 1"
      },
      "anchor": {
        "description": "0..1 pivot within the quad; 0.5 0.5 centers, 0.5 0 pins the bottom edge to the entity",
        "type": "vec2",
        "default": "0.5 0.5"
      },
      "color": {
        "description": "hex sRGB tint multiplied into the texture",
        "type": "color",
        "default": "0xffffff"
      },
      "opacity": {
        "description": "texture-alpha multiplier; under clip blend it shrinks the cutout, under alpha blend it fades",
        "type": "float",
        "default": "1"
      },
      "visible": {
        "description": "drawn when nonzero",
        "type": "float",
        "default": "1"
      },
      "billboard": {
        "description": "billboard orientation, a {@link SpriteBillboard} mode",
        "type": "screen | ylocked | world",
        "default": "screen"
      },
      "blend": {
        "description": "compositing, a {@link SpriteBlend} mode",
        "type": "clip | alpha",
        "default": "clip"
      },
      "fill": {
        "description": "leading fraction of the image shown, 0..1, along {@link fillMode}",
        "type": "float",
        "default": "1"
      },
      "fill-mode": {
        "description": "fill direction, a {@link SpriteFill} mode",
        "type": "none | radial | vertical | horizontal",
        "default": "none"
      }
    }
  },
  "tag": {
    "summary": "opt a Sear camera into the **id lane**: the `view.tag` target {@link PrepassSystem} fills. Unreal's `CustomStencil` generalized from an 8-bit stencil to a u32 lane; Bevy's prepass carries no id (it CPU-raycasts), so the id rides this single-sample pass because it's the same rasterization. A marker in the spirit of Bevy's `DepthPrepass` / `NormalPrepass`: add it to enable one extra camera output; omit it and the lane is absent (no target allocated). Per-camera, so a minimap opts out while the main view opts in. A consumer that reads `view.tag` (hover, outline, picking) wants this on its camera; the engine itself stays tag-agnostic.",
    "fields": {}
  },
  "text": {
    "summary": "a world-space text label anchored to an entity's {@link Transform}. Register the string with {@link text} and, optionally, a face with {@link font}; the glyphs lay out once and ride the entity's transform, so moving a label triggers no rebuild",
    "fields": {
      "content": {
        "description": "interned string id (see {@link text}); a scene's `content:` interns the raw string",
        "type": "float",
        "default": "0"
      },
      "font": {
        "description": "registered font id (see {@link font}); 0 is the default face",
        "type": "float",
        "default": "0"
      },
      "font-size": {
        "description": "world height of one em",
        "type": "float",
        "default": "1"
      },
      "opacity": {
        "description": "0..1 opacity multiplier",
        "type": "float",
        "default": "1"
      },
      "visible": {
        "description": "drawn when nonzero",
        "type": "float",
        "default": "1"
      },
      "anchor": {
        "description": "0..1 pivot within the label; 0 0 = bottom-left, 0.5 0.5 centered",
        "type": "vec2",
        "default": "0 0"
      },
      "color": {
        "description": "hex sRGB glyph color",
        "type": "color",
        "default": "0xffffff"
      }
    }
  },
  "textured": {
    "summary": "per-instance material id: an index into the per-material palette (`materialData`) the textured glTF surfaces read. A `slab(u32)` published as `\"materialIndex\"`, so a surface samples `albedo[materialData[id].layer]`. The firehose seam for textures: one more per-entity GPU column, no new draws. Distinct from sear's `Material` (the per-instance PBR knobs): this is the palette index, that is the shading params. A runtime-derived decoration. {@link GltfPlugin}'s route sync owns it (the id is union-palette-relative, so scenes never author it).",
    "fields": {
      "id": {
        "description": null,
        "type": "float",
        "default": "0"
      }
    }
  },
  "transform": {
    "summary": "per-entity transform: pos, rot, scale as direct {@link Quad} fields. Lanes are individually addressable as Singles (`Transform.pos.x.set(eid, v)`) and writable in bulk (`Transform.pos.set(eid, x, y, z, 0)`). The CPU writes flow through the slab plugin; SlabSystem flushes dirty slots once per frame and the compose pass derives world matrices into the `\"transforms\"` firehose.",
    "fields": {
      "pos": {
        "description": null,
        "type": "vec3",
        "default": "0 0 0"
      },
      "rot": {
        "description": null,
        "type": "vec3",
        "default": "0 0 0"
      },
      "scale": {
        "description": null,
        "type": "vec3",
        "default": "1 1 1"
      }
    }
  },
  "tween": {
    "summary": "animate one numeric field of a target over time, with an easing curve",
    "fields": {
      "state": {
        "description": "play control; a fresh tween defaults to playing",
        "type": "idle | playing | complete",
        "default": "playing"
      },
      "field": {
        "description": "the field path this tween writes, `component.field[.lane]` (`transform.pos.y`)",
        "type": "float",
        "default": "0"
      },
      "from": {
        "description": "start value; NaN captures the field's current value on the first frame (the `.to` convenience)",
        "type": "float",
        "default": "NaN"
      },
      "to": {
        "description": "end value at `duration`",
        "type": "float",
        "default": "0"
      },
      "duration": {
        "description": "length of the animation, seconds",
        "type": "float",
        "default": "1"
      },
      "elapsed": {
        "description": "current clock position, seconds; seekable: set it to scrub",
        "type": "float",
        "default": "0"
      },
      "easing": {
        "description": "easing curve, by id (the `ease-out-quad` names)",
        "type": "float",
        "default": "0"
      },
      "composite": {
        "description": "how the sampled value applies to the field: `replace` overwrites it, `add` layers a transient delta",
        "type": "replace | add",
        "default": "replace"
      },
      "fill": {
        "description": "whether the tween holds its value outside the active interval: `none`, `forwards`, `backwards`, `both`",
        "type": "none | forwards | backwards | both",
        "default": "forwards"
      },
      "loop": {
        "description": "rewind and replay on completion",
        "type": "float",
        "default": "0"
      },
      "sequence": {
        "description": "the timeline this tween plays on, or 0 for standalone; resolves via `@name` in a scene",
        "type": "entity",
        "default": "0"
      },
      "at": {
        "description": "start offset on the sequence's playhead, seconds",
        "type": "float",
        "default": "0"
      },
      "target": {
        "description": "the entity whose field is animated; resolves via `@name` in a scene",
        "type": "entity",
        "default": "0"
      }
    }
  },
  "voiced": {
    "summary": null,
    "fields": {}
  },
  "volumetric": {
    "summary": "opt a light ({@link PointLight}, {@link Spot}, or the {@link DirectionalLight} sun) into volumetric light shafts; presence is the switch, like {@link Spot}. On a point/spot light the light-compact pass flags its compacted entry; on the sun it flags the Lighting uniform. The `fog` march then scatters that light through the haze (a visible cone or sun shaft, shadowed by occluders if the light also carries a `Shadow`). With no `FogPlugin` / `Fog` singleton the flag is inert. The lit path is unchanged.",
    "fields": {}
  }
}
