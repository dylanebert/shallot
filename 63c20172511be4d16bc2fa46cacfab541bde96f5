stage: 1
finding: Importing src/harness/allocation.ts now statically imports ../project for CROSS_ORIGIN_ISOLATION; project/index.ts re-exports build.ts and toolchain.ts, both of which have runtime imports from vite. ESM evaluates those re-export dependencies on import, so the later dynamic import of ../project inside samplePage no longer keeps Vite unloaded for Node allocation rows, contrary to the lazy-build intent. The pre-change direct import from project/vite.ts did not statically import vite at runtime (its vite imports are type-only); this is a regression in the module-evaluation boundary.
